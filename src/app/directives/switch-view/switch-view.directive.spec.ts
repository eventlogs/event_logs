import { SwitchViewDirective } from "./switch-view.directive";

describe('SwitchViewDirective', () => {
    it('should create an instance', () => {
        const directive = new SwitchViewDirective();
        expect(directive).toBeTruthy();
    });
});
